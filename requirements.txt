## basic setup packages
wheel
setuptools
python-dotenv>=1.1.0
httpx_oauth
Jinja2>=3.1.0  # For template rendering

# for data/DB
pandas
simplejson

### for DB
pg8000
sqlalchemy
requests

# LLM
openai
pydantic==2.12.5
tiktoken
tenacity
slowapi
# Logfire
logfire
gevent>=22.10.0,<26  # 26.x has no aarch64 cp310 wheel; sdist hits libev configure errors

# Redis for caching and stuff
redis
# NOTE: not sure if needed
# hiredis-py 

# Settings
python-decouple

openpyxl

### Pydantic AI (Updated to resolve conflicts)
pydantic-ai==1.80.0
asgiref

# Web Server
gunicorn
uvicorn[standard]>=0.35  # ASGI server
uvicorn-worker

# FastAPI and dependencies
fastapi[standard]==0.135.2
aioredis
starlette
httpx

# Search and Data
marqo==3.11.0
rapidfuzz

# Authentication and Security
PyJWT
cryptography

# AWS and Services
boto3
# Geospatial
mapbox


# Async utilities
aiofiles==23.2.1
aiocache==0.12.3
pydantic-settings==2.9.1
python-dateutil
pytz

# Language codes
langcodes[data]==3.4.0

# Long-term farmer memory (mem0 + Qdrant)
# Pinned: the infer pipeline's behavior is version-sensitive — 2.0.12 is
# additive with exact-hash dedup (no UPDATE/DELETE/NOOP); code, prompts and
# tests are aligned to that. Review before upgrading.
mem0ai==2.0.12
qdrant-client

# Humanize time, date, numbers, etc.
humanize

# Langfuse
langfuse==3.14.6
